import { PageProperties } from "grapesjs";
import { createGlobalStyles } from "../components/global";

export const DefaultPages: PageProperties[] = [
  {
    id: "Default-start-screen",
    name: "Default Start screen",
    styles: createGlobalStyles(),
  },
];
